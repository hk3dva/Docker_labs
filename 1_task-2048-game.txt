1) Клонируем репозиторий
2) Пишем dockefile внутри скопированого 
3) создаем образ 
	docker build -t "название контейнера" "расположение dockfile" - если в текущей то "."
4) docker run -d -p "внешний порт":"внутрений порт" --name "название" "название контейнера"